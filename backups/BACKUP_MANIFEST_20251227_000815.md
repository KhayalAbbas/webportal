                                                                        # Backup Manifest - Pre Phase 3

**Date:** December 27, 2025  
**Time:** 00:08:15  
**Backup File:** `backup_pre_phase3_20251227_000815.zip`

## Test Status (before backup)
- Command: `python -m pytest -q`
- Result: 3 passed, 14 skipped (db/server gated by env: RUN_DB_TESTS=1, RUN_SERVER_TESTS=1)
- Notes: Only deprecation warnings (Pydantic v1 config/min_items, Starlette multipart, FastAPI `example` params).

## Scope
- Included: all project files under `C:\ATS` (app, tests, scripts, docs, migrations, manifests, configs, backups/*.md).
- Excluded: `.venv`, `__pycache__`, `.pytest_cache`, `*.pyc`, any `backups/*.zip`.

## Runtime Note
- Uvicorn server was running locally on http://127.0.0.1:8000/ during backup; no issues noted.

## Restore (PowerShell)
1) `Expand-Archive -Path "C:\ATS\backups\backup_pre_phase3_20251227_000815.zip" -DestinationPath "C:\ATS_RESTORED" -Force`
2) `cd C:\ATS_RESTORED`
3) `python -m venv .venv`
4) `.venv\Scripts\activate`
5) `pip install -r requirements.txt`
6) `alembic upgrade head`
7) `python -m pytest -q`

## Verification Checklist
- [ ] Pytest defaults: 3 passed, 14 skipped (no failures)
- [ ] Migrations applied (`alembic upgrade head`)
- [ ] Optional: start server `python -m uvicorn app.main:app --host 0.0.0.0 --port 8000`
